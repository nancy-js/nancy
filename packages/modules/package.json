{
  "name": "@nancy/modules",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "bun run ./build.ts",
    "dev": "bun run build --watch",
    "build:types": "bun tsc -p ./tsconfig.build.json"
  },
  "devDependencies": {
    "@nancy/apis": "workspace:*",
    "@nancy/core": "workspace:*",
    "@nancy/registry": "workspace:*",
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "valibot": "^0.17.0"
  }
}
